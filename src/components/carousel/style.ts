import styled from "styled-components";

export const CarouselContainer = styled.div`
  position: relative;
  height: inherit;
  width: inherit;
  margin: 0 auto;
  overflow: hidden;
  white-space: nowrap;
`;