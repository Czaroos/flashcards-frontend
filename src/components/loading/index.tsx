import React from "react";

import { Load, LoadContainer } from "./style";

export const Loading = () => {

    return (
        <LoadContainer>
            <Load >
                <div />
            </Load>
        </LoadContainer>
    )
}